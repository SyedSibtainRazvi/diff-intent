export { loadConfig, getConfig, clearConfigCache } from './loader';
export {
  Config,
  OutputFormat,
  DEFAULT_CONFIG,
  DEFAULT_IGNORE_PATTERNS,
  validateConfig,
  mergeConfig,
} from './schema';
